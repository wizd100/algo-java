package algo.java.permutation;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SwapTest {

    @Test
    void permutation() {
        Swap swap = new Swap();
        //assertArrayEquals(new int[]{1,2,3}, swap.permutation(new int[]{3,2,1}, 3, 3,3));
    }
}